# Nudity detection and censoring
nudenet>=3.4.0
onnxruntime>=1.18.0

# NOTE: opencv-python-headless is installed separately in Dockerfile
# to prevent opencv-python (GUI version) from being installed as a dependency

# Poe Protocol
fastapi-poe==0.0.48

# Web framework (included by fastapi-poe, but explicit for clarity)
fastapi==0.109.2
uvicorn[standard]==0.27.1

# RedGifs API
redgifs>=2.3.0

# Image processing
Pillow>=10.4.0

# HTTP client
httpx>=0.26.0

# Database
aiosqlite>=0.19.0

# Environment variables
python-dotenv>=1.0.0

# SSE support
sse-starlette==1.8.2
