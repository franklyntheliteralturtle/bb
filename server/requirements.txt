# =============================================================================
# IMPORTANT: opencv-python-headless MUST be listed before nudenet
# to prevent the GUI version (opencv-python) from being installed.
# The GUI version requires X11 libraries not available on headless servers.
# =============================================================================

# OpenCV headless (required by NudeNet, for server environments without display)
opencv-python-headless>=4.8.0

# Nudity detection and censoring
nudenet>=3.4.0
onnxruntime>=1.18.0

# Poe Protocol
fastapi-poe==0.0.48

# Web framework (included by fastapi-poe, but explicit for clarity)
fastapi==0.109.2
uvicorn[standard]==0.27.1

# RedGifs API
redgifs>=2.3.0

# Image processing
Pillow>=10.4.0

# HTTP client
httpx>=0.26.0

# Database
aiosqlite>=0.19.0

# Environment variables
python-dotenv>=1.0.0

# SSE support
sse-starlette==1.8.2
